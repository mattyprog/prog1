# Backend Studio Booking

- Express + Prisma (PostgreSQL)
- Per `.env`, vedi `.env.example`
- API su `http://localhost:4000`

## Comandi utili

```sh
npm run dev
npx prisma migrate dev --name init
npm run seed
```